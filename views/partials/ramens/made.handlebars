<li>
	{{!-- key matches column names in ramen table --}}
    <p>ID: {{this.id}} Order Name: {{this.name}}

	<button type="button" class="btn btn-success change-made" data-id="{{id}}" data-newmade="{{made}}"> {{!-- made is from index.handlebar line 19 --}}
		{{!-- like a if statement --}}
		{{#if made}}Made & Serve!{{else}}Make it Again!!{{/if}}
	</button>

	<button type="button" class="btn btn-danger delete-ramen " data-id="{{this.id}}">DELETE!</button>

	</p>
</li>